# Keep the default 127.0.0.1 when access is only through a local reverse proxy
# or SSH tunnel. Set one specific private interface address for direct intranet
# access; do not use 0.0.0.0 on an Internet-facing host.
PREDICTION_CONSOLE_BIND_ADDRESS=172.21.20.221
